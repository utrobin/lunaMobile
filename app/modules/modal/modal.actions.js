import { MODAL_ITEM_OPTIONS_OPENED, MODAL_ITEM_OPTIONS_CLOSED } from './modal.constants';

const openItemModal = { type: MODAL_ITEM_OPTIONS_OPENED };

const closeItemModal = { type: MODAL_ITEM_OPTIONS_CLOSED };

export {
  openItemModal,
  closeItemModal,
};
